<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>OkHttpClient</title>
      <link href="/2022/05/12/OkHttpClient/"/>
      <url>/2022/05/12/OkHttpClient/</url>
      
        <content type="html"><![CDATA[<p><strong>OkHttpClient</strong></p><p>OkHttpClient是一个高效的HTTP客户端</p><ul><li>支持HTTP/2，允许所有同一个主机地址的请求共享同一个 <code>socket</code> 连接</li><li>连接池减少请求延时</li><li>透明的GZIP压缩减少响应数据的大小</li><li>缓存响应内容，避免一些完全重复的请求</li><li>当网络出现问题时 <code>OkHttp</code>，会自动恢复一般的连接问题</li></ul><p><strong>依赖</strong></p><pre><code>    &lt;dependency&gt;            &lt;groupId&gt;com.squareup.okhttp3&lt;/groupId&gt;            &lt;artifactId&gt;okhttp&lt;/artifactId&gt;            &lt;version&gt;3.14.9&lt;/version&gt;    &lt;/dependency&gt;</code></pre><p><strong>简单使用</strong></p><pre><code>public class OkHttpClientPost {    //json传输方式    private final static MediaType JSON = MediaType.parse("application/json; charset=utf-8");    //获取okHttpClient对象    private final static OkHttpClient client = OkHttpClientObject.CLIENT            .getClientInstance();        /**     * post形式     */     public static String post(String url, String message) throws IOException {        //请求体传输json格式的数据        RequestBody requestBody = RequestBody.create(JSON, message);        //创建请求        Request request = new Request.Builder()                .url(url)                //.header("User-Agent", "*****")                //.addHeader("Accept", "*****")                .post(requestBody)                .build();         Call call = client.newCall(request);        okhttp3.Response response = call.execute();         if (response.isSuccessful()) {            return response.body().string();        } else {            throw new IOException("Unexpected code " + response);        }    }      /**     * post形式  multipart     */    public static String postMultipartFormData(String url, Map&lt;String,String&gt; paramMap) throws IOException {          MultipartBody.Builder builder = new MultipartBody.Builder().setType(MultipartBody.FORM);        Set&lt;String&gt; keySet = paramMap.keySet();        //拼接请求参数        for (String key : keySet) {            if (key.equals("image_file")) {                String image_file = paramMap.get("image_file");                byte[] bytes = new BASE64Decoder().decodeBuffer(image_file);                RequestBody fileBody = RequestBody.create(MediaType.parse("image/png"), bytes);                builder.addFormDataPart("image_file", "image_file", fileBody);            } else {                builder.addFormDataPart(key, paramMap.get(key));            }        }        MultipartBody body = builder.build();        //创建请求        Request request = new Request.Builder()                .url(url)                .method("POST", body)                .addHeader("Content-Type", "multipart/form-data")                .build();         Call call = client.newCall(request);        okhttp3.Response response = call.execute();        if (response.isSuccessful()) {            return response.body().string();        } else {              int code = response.code();             String message = response.message();             log.error("OCR识别 响应状态码：{}  错误信息：{}",code,message);            throw new IOException("Unexpected code " + response);        }    }           /**     * post形式     */    public static String postHeader(String url, String message,String header) throws IOException {        //请求体传输json格式的数据        RequestBody requestBody = RequestBody.create(JSON, message);        //创建请求        Request request = new Request.Builder()                .url(url)                .header("Authorization", header)                //.addHeader("Accept", "*****")                .post(requestBody)                .build();         Call call = client.newCall(request);        okhttp3.Response response = call.execute();         if (response.isSuccessful()) {            return response.body().string();        } else {            throw new IOException("Unexpected code " + response);        }    } }</code></pre>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>RabbitMQ集群搭建</title>
      <link href="/2022/03/26/RabbitMQ%E9%9B%86%E7%BE%A4%E6%90%AD%E5%BB%BA/"/>
      <url>/2022/03/26/RabbitMQ%E9%9B%86%E7%BE%A4%E6%90%AD%E5%BB%BA/</url>
      
        <content type="html"><![CDATA[<h2 id="LINUX-搭建集群"><a href="#LINUX-搭建集群" class="headerlink" title="LINUX 搭建集群"></a>LINUX 搭建集群</h2><p>修改每台机器的主机名称</p><pre><code>vi /etc/hostname </code></pre><p>在每台服务器中配置其他服务器 hosts文件，让各个节点都能互相识别对方</p><pre><code>vi /etc/hosts</code></pre><p>下图效果</p><p><img src="https://liang-git.oss-cn-beijing.aliyuncs.com/hexo/rabbitmq%E9%9B%86%E7%BE%A4%E9%85%8D%E7%BD%AE.png"></p><p>确保各个节点的cookie文件使用的是同一个值 {@node2[服务主机名称] }</p><pre><code>scp /var/ib/rabbitmq/.erlang.cookie root@node2:/ar/ib/rabbitmg/.erlang.cookie I</code></pre><p>启动RabbitMQ服务,顺带启动Erlang虚拟机和RbbitMQ应用服务</p><pre><code>rabbitmq-server -detached</code></pre><p>在其他主机执行</p><pre><code>rabbitmqctl stop_ app # 关闭服务  (rabbitmqctl stop会将Erlang 虚拟机关闭, rabbitmqct! stop. app只关闭RabbitMQ服务)rabbitmqctl reset # 重置rabbitmqctl join. _cluster rabbit@node1 #[主机名称] #加入到第一台主机中rabbitmqctl start, app(只启动应用服务)</code></pre><p>查看集群状态</p><pre><code>rabbitmqctl cluster_ status</code></pre><p>解除集群节点(node2和node3机器分别执行)【脱离集群】</p><pre><code>rabbitmqctl stop_ _apprabbitmqctl resetrabbitmqct! start_ apprabbitmgctl cluster_ statusrabbitmgctl forget. _cluster node rabbit@node2(node1机器上执行) # node1主机忘掉node2主机</code></pre><h2 id="DOCKER-搭建集群"><a href="#DOCKER-搭建集群" class="headerlink" title="DOCKER 搭建集群"></a>DOCKER 搭建集群</h2><p>版本为 <strong>rabbitmq:3.7-management</strong> </p><ul><li>执行以下命名创建docker镜像</li></ul><pre><code>docker run -d --hostname rabbitmq01 --name rabbitmqCluster01 -p 15672:15672 -p 5672:5672 -p 1883:1883 -e RABBITMQ_ERLANG_COOKIE='rabbitmqCookie' rabbitmq:3.7-managementdocker run -d --hostname rabbitmq02 --name rabbitmqCluster02 -p 15673:15672 -p 5673:5672 -p 1884:1883 -e RABBITMQ_ERLANG_COOKIE='rabbitmqCookie' --link rabbitmqCluster01:rabbitmq01 rabbitmq:3.7-managementdocker run -d --hostname rabbitmq03 --name rabbitmqCluster03 -p 15674:15672 -p 5674:5672 -p 1885:1883 -e RABBITMQ_ERLANG_COOKIE='rabbitmqCookie' --link rabbitmqCluster01:rabbitmq01 --link rabbitmqCluster02:rabbitmq02  rabbitmq:3.7-management</code></pre><p>主要参数：</p><ul><li>-p 15672:15672 management 界面管理访问端口</li><li>-p 5672:5672 amqp 访问端口</li><li>-p 1883:1883 mqtt访问端口  也可以把容器里的数据共享到宿主机上</li><li>-v /home/soft/rabbitmqcluster/rabbitmq01:/var/lib/rabbitmq</li><li>–link 容器之间连接<br>Erlang Cookie 值必须相同，也就是一个集群内 RABBITMQ_ERLANG_COOKIE 参数的值必须相同。因为 RabbitMQ 是用Erlang实现的，Erlang Cookie 相当于不同节点之间通讯的密钥，Erlang节点通过交换 Erlang Cookie 获得认证。</li></ul><p>容器节点加入集群</p><pre><code>进入第一个容器docker exec -it rabbitmqCluster01 bashrabbitmqctl stop_apprabbitmqctl resetrabbitmqctl start_appexit</code></pre><pre><code>进入第二个容器docker exec -it rabbitmqCluster02 bashrabbitmqctl stop_apprabbitmqctl reset加入集群rabbitmqctl join_cluster --ram rabbit@rabbitmq01rabbitmqctl start_appexit</code></pre><pre><code>进入第三个容器docker exec -it rabbitmqCluster03 bashrabbitmqctl stop_apprabbitmqctl reset加入集群rabbitmqctl join_cluster --ram rabbit@rabbitmq01rabbitmqctl start_appexit</code></pre><p>–ram 表示设置为内存节点，忽略此参数默认为磁盘节点。该配置启动了3个节点，1个磁盘节点和2个内存节点。</p><h3 id="镜像集群模式"><a href="#镜像集群模式" class="headerlink" title="镜像集群模式"></a>镜像集群模式</h3><h4 id="登录rabbitmq管理页面-——-gt-Admin-——-gt-Policies-——-gt-Add-x2F-update-a-policy"><a href="#登录rabbitmq管理页面-——-gt-Admin-——-gt-Policies-——-gt-Add-x2F-update-a-policy" class="headerlink" title="登录rabbitmq管理页面 ——> Admin ——> Policies ——> Add / update a policy"></a>登录rabbitmq管理页面 ——&gt; Admin ——&gt; Policies ——&gt; Add / update a policy</h4><p><img src="https://liang-git.oss-cn-beijing.aliyuncs.com/hexo/%E9%95%9C%E5%83%8F%E9%9B%86%E7%BE%A4%E6%A8%A1%E5%BC%8F.png"></p><h4 id="使用命令创建"><a href="#使用命令创建" class="headerlink" title="使用命令创建"></a>使用命令创建</h4><pre><code># 进入容器docker exec -it rabbitmqCluster01 bash# 创建策略rabbitmqctl set_policy ha-all "^" '{"ha-mode":"all"}'rabbitmqctl set_policy-p/ha-all"^"'{"ha-mode":"all"}'rabbitmqctl set_policy-p/ha-all"^message"'{"ha-mode":"all"}'# “^message” 这个规则要根据自己修改，这个是指同步“message”开头的队列名称，我们配置时使用的应用于所有队列，所以表达式为“^”     </code></pre><h2 id="最终效果"><a href="#最终效果" class="headerlink" title="最终效果"></a>最终效果</h2><p><img src="https://liang-git.oss-cn-beijing.aliyuncs.com/hexo/rabbitmq%E9%9B%86%E7%BE%A4.png"></p>]]></content>
      
      
      <categories>
          
          <category> RabbitMQ </category>
          
          <category> 集群搭建 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> RabbitMQ </tag>
            
            <tag> 学习笔记 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>支付宝支付接口</title>
      <link href="/2022/03/11/%E6%94%AF%E4%BB%98%E5%AE%9D%E6%94%AF%E4%BB%98%E6%8E%A5%E5%8F%A3/"/>
      <url>/2022/03/11/%E6%94%AF%E4%BB%98%E5%AE%9D%E6%94%AF%E4%BB%98%E6%8E%A5%E5%8F%A3/</url>
      
        <content type="html"><![CDATA[<p><strong>官网：</strong> <a href="https://open.alipay.com/">网页&amp;移动应用</a>  </p><span id="more"></span><h2 id="接口使用"><a href="#接口使用" class="headerlink" title="接口使用"></a>接口使用</h2><h3 id="支付流程"><a href="#支付流程" class="headerlink" title="支付流程"></a>支付流程</h3><ol><li><p>前端请求访问控制层 “/alipay” 接口</p><p> 1.1 初始化支付 DefaultAlipayClient 对象</p><p> 1.2 使用 AlipayTradePagePayRequest（网页支付请求对象） 对象设置请求参数</p><ul><li><p>设置返回地址和异步返回地址</p></li><li><p>设置业务内容（json）</p></li></ul><p> 1.3 获取请求体的html代码</p><p> 1.4 响应请求体后通过</p></li><li><p>接受返回结果<br> 2.1 支付宝支付后会调用返回接口（同步接口异常会调用异步返回接口）<br> 2.2 通过Map获取参数<br><strong>2.3 验签 AlipaySignature 对象的 rsaCheckV1() 验签</strong>  </p><ul><li><strong>验证成功 执行自定义的代码逻辑</strong></li><li><strong>验证失败 响应错误信息</strong></li></ul></li></ol><h3 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h3><ul><li>引入依赖</li></ul><pre><code>&lt;dependency&gt;    &lt;groupId&gt;com.alipay.sdk&lt;/groupId&gt;    &lt;artifactId&gt;alipay-sdk-java&lt;/artifactId&gt;    &lt;version&gt;4.13.58.ALL&lt;/version&gt;&lt;/dependency&gt;&lt;dependency&gt;    &lt;groupId&gt;org.apache.httpcomponents&lt;/groupId&gt;    &lt;artifactId&gt;httpclient&lt;/artifactId&gt;    &lt;version&gt;4.5.3&lt;/version&gt;&lt;/dependency&gt;</code></pre><ul><li>导入配置类(将准备的参数赋值给指定常量)</li></ul><pre><code>/* * *类名：AlipayConfig *功能：基础配置类 *详细：设置帐户有关信息及返回路径 *说明： *以下代码只是为了方便商户测试而提供的样例代码，商户可以根据自己网站的需要，按照技术文档编写,并非一定要使用该代码。 *该代码仅供学习和研究支付宝接口使用，只是提供一个参考。 */public class AlipayConfig {    // 商户appid , 收款账号既是您的APPID对应支付宝账号    public static String APPID = "";    // 私钥 pkcs8格式的    public static String RSA_PRIVATE_KEY = "";    // 服务器异步通知页面路径 需http://或者https://格式的完整路径，不能加?id=123这类自定义参数，必须外网可以正常访问    public static String notify_url = "";    // 页面跳转同步通知页面路径 需http://或者https://格式的完整路径，不能加?id=123这类自定义参数，必须外网可以正常访问 商户可以自定义同步跳转地址    public static String return_url = "";    // 请求网关地址    public static String URL = "";    // 编码    public static String CHARSET = "UTF-8";    // 返回格式    public static String FORMAT = "json";    // 支付宝公钥 ， 查看地址：https://openhome.alipay.com/platform/keyManage.htm 对应APPID下的支付宝公钥。    public static String ALIPAY_PUBLIC_KEY = "";    // 日志记录目录    public static String log_path = "/log";    // 签名方式 RSA2    public static String SIGNTYPE = "RSA2";}</code></pre><ul><li>日志工具类</li></ul><pre><code>import java.io.FileWriter;import java.io.IOException;public class LogFile {    /**     * 写日志，方便测试（看网站需求，也可以改成把记录存入数据库）     * @param sWord 要写入日志里的文本内容     */    public static void logResult(String sWord) {        FileWriter writer = null;        try {            writer = new FileWriter(AlipayConfig.log_path + "alipay_log_" + System.currentTimeMillis()+".txt");            writer.write(sWord);        } catch (Exception e) {            e.printStackTrace();        } finally {            if (writer != null) {                try {                    writer.close();                } catch (IOException e) {                    e.printStackTrace();                }            }        }    }}</code></pre><ul><li>官方demo <ul><li>JSP代码</li></ul></li></ul><p><button onclick="window.open('/alipay/notify_url.jsp')">手机网站2.0支付异步通知</button><br><button onclick="window.open('/alipay/return_url.jsp')">手机网站2.0支付同步通知</button><br><button onclick="window.open('/alipay/wappay/pay.jsp')">手机网站2.0支付(接口名：alipay.trade.wap.pay)</button><br><button onclick="window.open('/alipay/wappay/query.jsp')">手机网站2.0订单查询 (接口名：alipay.trade.query)</button><br><button onclick="window.open('/alipay/wappay/refund.jsp')">手机网站2.0订单退款  (接口名：alipay.trade.refund)</button><br><button onclick="window.open('/alipay/wappay/refundquery.jsp')">手机网站2.0订单退款查询(接口名：alipay.trade.fastpay.refund.query)</button><br><button onclick="window.open('/alipay/wappay/downloadurl.jsp')">手机网站2.0账单下载(接口名：alipay.data.dataservice.bill.downloadurl.query)</button></p><ul><li>SDK使用</li></ul><pre><code>import com.alipay.api.AlipayApiException;import com.alipay.api.AlipayClient;import com.alipay.api.DefaultAlipayClient;import com.alipay.api.domain.AlipayTradeWapPayModel;import com.alipay.api.internal.util.AlipaySignature;import com.alipay.api.request.AlipayTradePagePayRequest;import org.slf4j.Logger;import org.slf4j.LoggerFactory;import org.springframework.web.bind.annotation.GetMapping;import org.springframework.web.bind.annotation.PostMapping;import org.springframework.web.bind.annotation.RequestMapping;import org.springframework.web.bind.annotation.RestController;import javax.servlet.http.HttpServletRequest;import java.io.UnsupportedEncodingException;import java.util.HashMap;import java.util.Iterator;import java.util.Map;/** * @author: liangbaichuan * @Title: AlipayController * @create: 2018-03-11 11:14 */@RestControllerpublic class AlipayController {    private static final Logger log = LoggerFactory.getLogger(AlipayController.class);    @PostMapping("/alipay")    public String alipay(HttpServletRequest request) throws UnsupportedEncodingException {        // 根据订单编号查询订单表获取订单金额        // 初始化AlipayClient        AlipayClient alipayClient = new DefaultAlipayClient(                AlipayConfig.URL,                AlipayConfig.APPID,                AlipayConfig.RSA_PRIVATE_KEY,                AlipayConfig.FORMAT,                AlipayConfig.CHARSET,                AlipayConfig.ALIPAY_PUBLIC_KEY,                AlipayConfig.SIGNTYPE        );        // 设置请求参数        AlipayTradePagePayRequest alipayRequest = new AlipayTradePagePayRequest();        // 设置同步地址        alipayRequest.setReturnUrl(AlipayConfig.return_url);        // 设置异步通知地址        alipayRequest.setNotifyUrl(AlipayConfig.notify_url);        // 商户订单号，商户网站订单系统中唯一订单号，必填        String out_trade_no = new String(request.getParameter("WIDout_trade_no").getBytes("ISO-8859-1"),"UTF-8");        // 订单名称，必填        String subject = new String(request.getParameter("WIDsubject").getBytes("ISO-8859-1"),"UTF-8");        System.out.println(subject);        // 付款金额，必填        String total_amount=new String(request.getParameter("WIDtotal_amount").getBytes("ISO-8859-1"),"UTF-8");        // 商品描述，可空        String body = new String(request.getParameter("WIDbody").getBytes("ISO-8859-1"),"UTF-8");        // 超时时间 可空        String timeout_express="2m";        // 销售产品码 必填        String product_code="QUICK_WAP_WAY";        // 封装请求支付信息        AlipayTradeWapPayModel model=new AlipayTradeWapPayModel();        model.setOutTradeNo(out_trade_no);        model.setSubject(subject);        model.setTotalAmount(total_amount);        model.setBody(body);        model.setTimeoutExpress(timeout_express);        model.setProductCode(product_code);        alipayRequest.setBizModel(model);        // 请求        String result = "";        try {            result = alipayClient.pageExecute(alipayRequest).getBody();            System.out.println(result);        } catch (AlipayApiException e) {            e.printStackTrace();        }        // 输出        return result;    }    @GetMapping("/returnResult")    public String returnResult(HttpServletRequest request) throws UnsupportedEncodingException {        log.info("收到同步回调");        //获取支付宝GET过来反馈信息        Map&lt;String,String&gt; params = new HashMap&lt;String,String&gt;();        Map requestParams = request.getParameterMap();        for (Iterator iter = requestParams.keySet().iterator(); iter.hasNext();) {            String name = (String) iter.next();            String[] values = (String[]) requestParams.get(name);            String valueStr = "";            for (int i = 0; i &lt; values.length; i++) {                valueStr = (i == values.length - 1) ? valueStr + values[i]                        : valueStr + values[i] + ",";            }            //乱码解决，这段代码在出现乱码时使用。如果mysign和sign不相等也可以使用这段代码转化            valueStr = new String(valueStr.getBytes("ISO-8859-1"), "utf-8");            params.put(name, valueStr);        }        //获取支付宝的通知返回参数，可参考技术文档中页面跳转同步通知参数列表(以下仅供参考)//        //商户订单号        String out_trade_no = new String(request.getParameter("out_trade_no").getBytes("ISO-8859-1"),"UTF-8");        //支付宝交易号        String trade_no = new String(request.getParameter("trade_no").getBytes("ISO-8859-1"),"UTF-8");        //获取支付宝的通知返回参数，可参考技术文档中页面跳转同步通知参数列表(以上仅供参考)//        //计算得出通知验证结果        //boolean AlipaySignature.rsaCheckV1(Map&lt;String, String&gt; params, String publicKey, String charset, String sign_type)        boolean verify_result = false;        try {             verify_result = AlipaySignature.rsaCheckV1(params, AlipayConfig.ALIPAY_PUBLIC_KEY, AlipayConfig.CHARSET, "RSA2");        } catch (AlipayApiException e) {            e.printStackTrace();        }        if (verify_result){ //验证成功            return "&lt;script&gt;location.href='success.html'&lt;/script&gt;";        }else{ //验证失败            return "&lt;script&gt;location.href='error.html'&lt;/script&gt;";        }    }    @RequestMapping("/notifyResult")    public String notifyResult(HttpServletRequest request) throws UnsupportedEncodingException {        //获取支付宝POST过来反馈信息        Map&lt;String,String&gt; params = new HashMap&lt;String,String&gt;();        Map requestParams = request.getParameterMap();        for (Iterator iter = requestParams.keySet().iterator(); iter.hasNext();) {            String name = (String) iter.next();            String[] values = (String[]) requestParams.get(name);            String valueStr = "";            for (int i = 0; i &lt; values.length; i++) {                valueStr = (i == values.length - 1) ? valueStr + values[i]                        : valueStr + values[i] + ",";            }            //乱码解决，这段代码在出现乱码时使用。如果mysign和sign不相等也可以使用这段代码转化            //valueStr = new String(valueStr.getBytes("ISO-8859-1"), "gbk");            params.put(name, valueStr);        }        //获取支付宝的通知返回参数，可参考技术文档中页面跳转同步通知参数列表(以下仅供参考)//        //商户订单号        String out_trade_no = new String(request.getParameter("out_trade_no").getBytes("ISO-8859-1"),"UTF-8");        //支付宝交易号        String trade_no = new String(request.getParameter("trade_no").getBytes("ISO-8859-1"),"UTF-8");        //交易状态        String trade_status = new String(request.getParameter("trade_status").getBytes("ISO-8859-1"),"UTF-8");        //获取支付宝的通知返回参数，可参考技术文档中页面跳转同步通知参数列表(以上仅供参考)//        //计算得出通知验证结果        //boolean AlipaySignature.rsaCheckV1(Map&lt;String, String&gt; params, String publicKey, String charset, String sign_type)        boolean verify_result  = false ;        try {            verify_result = AlipaySignature.rsaCheckV1(params, AlipayConfig.ALIPAY_PUBLIC_KEY, AlipayConfig.CHARSET, "RSA2");        } catch (AlipayApiException e) {            e.printStackTrace();        }        if (verify_result){ //验证成功            //——请根据您的业务逻辑来编写程序（以下代码仅作参考）——            if(trade_status.equals("TRADE_FINISHED")){                //判断该笔订单是否在商户网站中已经做过处理                //如果没有做过处理，根据订单号（out_trade_no）在商户网站的订单系统中查到该笔订单的详细，并执行商户的业务程序                //请务必判断请求时的total_fee、seller_id与通知时获取的total_fee、seller_id为一致的                //如果有做过处理，不执行商户的业务程序                //注意：                //如果签约的是可退款协议，退款日期超过可退款期限后（如三个月可退款），支付宝系统发送该交易状态通知                //如果没有签约可退款协议，那么付款完成后，支付宝系统发送该交易状态通知。            } else if (trade_status.equals("TRADE_SUCCESS")){                //判断该笔订单是否在商户网站中已经做过处理                //如果没有做过处理，根据订单号（out_trade_no）在商户网站的订单系统中查到该笔订单的详细，并执行商户的业务程序                //请务必判断请求时的total_fee、seller_id与通知时获取的total_fee、seller_id为一致的                //如果有做过处理，不执行商户的业务程序                //注意：                //如果签约的是可退款协议，那么付款完成后，支付宝系统发送该交易状态通知。            }            return  "";        }else{            return null;        }    }}</code></pre><ul><li>配置类中的常量可以通过配置文件注入</li></ul>]]></content>
      
      
      <categories>
          
          <category> 第三方支付 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 第三方支付 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hexo 博客搭建</title>
      <link href="/2021/03/10/Hexo%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA/"/>
      <url>/2021/03/10/Hexo%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA/</url>
      
        <content type="html"><![CDATA[<span id="more"></span><h2 id="安装Nodejs"><a href="#安装Nodejs" class="headerlink" title="安装Nodejs"></a>安装Nodejs</h2><pre><code>node -v    #查看node版本npm -v    #查看npm版本</code></pre><h3 id="安装淘宝-CNPM"><a href="#安装淘宝-CNPM" class="headerlink" title="安装淘宝 CNPM"></a>安装淘宝 CNPM</h3><pre><code>npm install -g cnpm --registry=http://registry.npm.taobao.org    #安装淘宝的cnpm 管理器cnpm -v    #查看cnpm版本</code></pre><h3 id="安装-HEXO-框架"><a href="#安装-HEXO-框架" class="headerlink" title="安装 HEXO 框架"></a>安装 HEXO 框架</h3><pre><code>cnpm install -g hexo-cli    #安装hexo框架hexo -v    #查看hexo版本</code></pre><h3 id="创建-blog"><a href="#创建-blog" class="headerlink" title="创建 blog"></a>创建 blog</h3><pre><code>mkdir blog    #创建blog目录cd blog     #进入blog目录sudo hexo init     #生成博客 初始化博客hexo s    #启动本地博客服务http://localhost:4000/    #本地访问地址</code></pre><h3 id="创建新文章"><a href="#创建新文章" class="headerlink" title="创建新文章"></a>创建新文章</h3><pre><code>hexo n "我的第一篇文章"  #创建新的文章  ""里面的为blog名称</code></pre><h3 id="返回blog目录"><a href="#返回blog目录" class="headerlink" title="返回blog目录"></a>返回blog目录</h3><pre><code>hexo clean #清理hexo g #生成</code></pre><h3 id="创建GitHub仓库"><a href="#创建GitHub仓库" class="headerlink" title="创建GitHub仓库"></a>创建GitHub仓库</h3><p><img src="/img/hexo_gitHob.png"></p><pre><code>YourGithubName.github.io</code></pre><h3 id="安装git部署插件"><a href="#安装git部署插件" class="headerlink" title="安装git部署插件"></a>安装git部署插件</h3><pre><code>cnpm install --save hexo-deployer-git #在blog目录下安装git部署插件</code></pre><hr><h3 id="配置-config-yml"><a href="#配置-config-yml" class="headerlink" title="配置_config.yml"></a>配置_config.yml</h3><pre><code># Deployment## Docs: https://hexo.io/docs/deployment.htmldeploy:      type: git     repo: https://github.com/YourGithubName/YourGithubName.github.io.git      branch: master</code></pre><pre><code>hexo d    #部署到Github仓库里https://YourGithubName.github.io/  #访问这个地址可以查看博客</code></pre><h3 id="主题"><a href="#主题" class="headerlink" title="主题"></a>主题</h3><pre><code>git clone https://github.com/litten/hexo-theme-yilia.git themes/yilia  #下载yilia主题到本地</code></pre><p>修改hexo根目录下的 _config.yml 文件 ： theme: yilia</p><h3 id="Hexo-三连"><a href="#Hexo-三连" class="headerlink" title="Hexo 三连"></a>Hexo 三连</h3><pre><code>hexo clean #清理hexo g #生成hexo s #运行</code></pre><pre><code>hexo c    #清理一下hexo g    #生成hexo d    #部署到远程Github仓库</code></pre>]]></content>
      
      
      <categories>
          
          <category> hexo </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 博客搭建 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>RabbitMQ 如何保证消息的可靠传输</title>
      <link href="/2020/07/11/RabbitMQ%20%E5%A6%82%E4%BD%95%E4%BF%9D%E8%AF%81%E6%B6%88%E6%81%AF%E7%9A%84%E5%8F%AF%E9%9D%A0%E4%BC%A0%E8%BE%93/"/>
      <url>/2020/07/11/RabbitMQ%20%E5%A6%82%E4%BD%95%E4%BF%9D%E8%AF%81%E6%B6%88%E6%81%AF%E7%9A%84%E5%8F%AF%E9%9D%A0%E4%BC%A0%E8%BE%93/</url>
      
        <content type="html"><![CDATA[<h4 id="可靠传输"><a href="#可靠传输" class="headerlink" title="可靠传输"></a>可靠传输</h4><p>在RabbitMQ中，一个消息从产生到被消费大致会经过三个步骤，生产者生产消息，消息投递到RabbitMQ，RabbitMQ再将消息推送到消费者（或消费者拉取），最终消费者将这条消息成功消费。消息的可靠传输，就是确保消息能够百分百从生产者发送到服务器，在从服务器发送到消费者。</p><p>所以消息丢失也可以划分为三种情况——生产者、消息队列、消费者。</p><span id="more"></span><h4 id="生产者投递消息失败"><a href="#生产者投递消息失败" class="headerlink" title="生产者投递消息失败"></a>生产者投递消息失败</h4><h5 id="事务机制"><a href="#事务机制" class="headerlink" title="事务机制"></a>事务机制</h5><p>使用 RabbitMQ 的事务功能，此时可以选择用 RabbitMQ 提供的事务功能，就是生产者发送数据之前开启 RabbitMQ 事务 channel.txSelect，然后发送消息，如果消息没有成功被 RabbitMQ 接收到，那么生产者会收到异常报错，此时就可以回滚事务 channel.txRollback，然后重试发送消息；如果收到了消息，那么可以提交事务 channel.txCommit。</p><pre class=" language-java"><code class="language-java">`<span class="token comment" spellcheck="true">// 开启事务`</span>`channel<span class="token punctuation">.</span><span class="token function">txSelect</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>``<span class="token keyword">try</span> <span class="token punctuation">{</span>`    `<span class="token comment" spellcheck="true">// 发送消息`</span>`<span class="token punctuation">}</span> <span class="token keyword">catch</span><span class="token punctuation">(</span>Exception e<span class="token punctuation">)</span> <span class="token punctuation">{</span>`    `channel<span class="token punctuation">.</span><span class="token function">txRollback</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>`    `<span class="token comment" spellcheck="true">// 重发消息`</span>`<span class="token punctuation">}</span>``<span class="token comment" spellcheck="true">// 提交事务`</span>`channel<span class="token punctuation">.</span><span class="token function">txCommit</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>`</code></pre><p>事务机制可以确保生产者投递消息成功，但是这种方式有比较大的缺点，太耗性能。</p><h5 id="confirm机制"><a href="#confirm机制" class="headerlink" title="confirm机制"></a>confirm机制</h5><p>要确保 RabbitMQ 生产者的消息正确投递，可以开启 confirm 模式，在生产者端设置开启 confirm 模式之后，你每次写的消息都会分配一个唯一的 id，然后如果写入了 RabbitMQ 中，RabbitMQ 会给你回传一个 ACK 消息，告诉你说这个消息。</p><p>如果 RabbitMQ 没能处理这个消息，会回调你一个 nack 接口，告诉你这个消息接收失败，你可以重试。而且你可以结合这个机制自己在内存里维护每个消息 id 的状态，如果超过一定时间还没接收到这个消息的回调，那么你可以重发。</p><pre class=" language-java"><code class="language-java"><span class="token keyword">try</span> <span class="token punctuation">{</span>    channel<span class="token punctuation">.</span><span class="token function">confirmSelect</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment" spellcheck="true">//将信道置为 publisher confirm 模式</span>    <span class="token comment" spellcheck="true">// 之后正常发送消息</span>    channel<span class="token punctuation">.</span><span class="token function">basicPushlish</span><span class="token punctuation">(</span><span class="token string">"exchange"</span><span class="token punctuation">,</span> <span class="token string">"routingKey"</span><span class="token punctuation">,</span> null<span class="token punctuation">,</span>                           <span class="token string">"publisher confirm test"</span><span class="token punctuation">.</span><span class="token function">getBytes</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>    <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span>channel<span class="token punctuation">.</span><span class="token function">waitFormConfirms</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>        System<span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token string">"Send message failed"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>        <span class="token comment" spellcheck="true">// do something else...</span>    <span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">InterruptedException</span> e<span class="token punctuation">)</span> <span class="token punctuation">{</span>        e<span class="token punctuation">.</span><span class="token function">printStackTrace</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>    <span class="token punctuation">}</span><span class="token punctuation">}</span></code></pre><p>事务机制和 confirm 机制最大的不同在于，事务机制是同步的，你提交一个事务之后会阻塞在那儿，但是 confirm 机制是异步的，你发送个消息之后就可以发送下一个消息，然后那个消息 RabbitMQ 接收之后会异步回调你一个接口通知你这个消息接收到了。</p><p>所以一般生产者到 RabbitMQ 这块避免数据丢失，会采用 confirm 机制更多些。</p><h5 id="消息队列自身丢失"><a href="#消息队列自身丢失" class="headerlink" title="消息队列自身丢失"></a>消息队列自身丢失</h5><p>就是 RabbitMQ 自己弄丢了数据，这个你必须开启 RabbitMQ 的持久化，就是消息写入之后会持久化到磁盘，哪怕是 RabbitMQ 自己挂了，恢复之后会自动读取之前存储的数据，一般数据不会丢。除非极其罕见的是，RabbitMQ 还没持久化，自己就挂了，可能导致少量数据丢失，但是这个概率较小。</p><p>设置持久化有两个步骤：</p><ul><li><strong>创建 queue 的时候将其设置为持久化</strong></li></ul><p>这样就可以保证 RabbitMQ 持久化 queue 的元数据，但是不会持久化 queue 里的数据。</p><ul><li><strong>发送消息时将消息的 deliveryMode 设置为 2</strong></li></ul><p>就是将消息设置为持久化，此时 RabbitMQ 就会将消息持久化到磁盘上去。</p><p>必须要同时设置这两个持久化才行，RabbitMQ 哪怕是挂了，再次重启，也会从磁盘上重启恢复 queue，恢复这个 queue 里的数据。</p><p>同时持久化也可以跟生产者那边的 confirm 机制配合起来，只有消息被持久化到磁盘后，才会通知生产者 ack 了，所以哪怕是在持久化到磁盘之前，RabbitMQ 挂了，数据丢了，生产者收不到 ACK，你也是可以自己重发的。</p><p>注意，哪怕是你给 RabbitMQ 开启了持久化机制，也有一种可能，就是这个消息写到了 RabbitMQ 中，但是还没来得及持久化到磁盘上，结果不巧，此时 RabbitMQ 挂了，就会导致内存中的数据丢失。</p><h5 id="消费者宕机"><a href="#消费者宕机" class="headerlink" title="消费者宕机"></a>消费者宕机</h5><p>对于消费者端所产生的情况就是：消费者成功接收到消息，但是还未将消息处理完毕就宕机了。针对这种情况，可以利用 RabbitMQ 提供的<strong>消息确认机制</strong>。</p><p>为了保证消息从队列可靠地到达消费者，RabbitMQ 提供了消息确认机制（message acknowledgement）。消费者在订阅队列时，可以指定 autoAck 参数：</p><ul><li><p>当 autoAck 等于 true 时，RabbitMQ 会自动把发送出去的消息置为确认，然后从内存（或者磁盘）中删除，而不管消费者是否真正地消费到了这些消息；</p></li><li><p>当 autoAck 等于 false 时，RabbitMQ 会等待消费者显示地回复确认信号后才从内存（后者磁盘）中移去消息（实质上是先打上删除标记，之后再删除）。</p></li></ul><p>所以对于消费者可能发生宕机地情况，我们可以将 autoAck 参数置为 false，消费者就有足够的时间处理这条消息，不用担心处理消息过程中消费者进程挂掉后消息丢失的问题，因为 RabbitMQ 会一直等待并且持有这条消息，直到消费者显示调用 Basic.Ack 命令为止。</p><p>当 autoAck 参数设置为 false，对于 RabbitMQ 服务端而言，队列中的消息分成了两个部分：一部分是等待投递给消费者的消息，另一部分是已经投递给消费者，但是还没有收到消费者确认信号的消息。如果 RabbitMQ 一直没有收到消费者的确认信号，并且消费此消息的消费者已经断开连接，则 RabbitMQ 会安排该消息重新进入队列，等待投递给下一个消费者，当然也可能还是原来的那个消费者。</p><p><a href="https://blog.csdn.net/Allen_Adolph/article/details/109055174">原文链接</a></p>]]></content>
      
      
      <categories>
          
          <category> RabbitMQ </category>
          
      </categories>
      
      
        <tags>
            
            <tag> RabbitMQ </tag>
            
            <tag> 学习笔记 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>RabbitMQ发布确认</title>
      <link href="/2020/03/22/RabbitMQ%E5%8F%91%E5%B8%83%E7%A1%AE%E8%AE%A4/"/>
      <url>/2020/03/22/RabbitMQ%E5%8F%91%E5%B8%83%E7%A1%AE%E8%AE%A4/</url>
      
        <content type="html"><![CDATA[<h2 id="消息确认机制-Confirm模式"><a href="#消息确认机制-Confirm模式" class="headerlink" title="消息确认机制(Confirm模式)"></a>消息确认机制(Confirm模式)</h2><h3 id="前提条件"><a href="#前提条件" class="headerlink" title="前提条件"></a>前提条件</h3><ul><li><p><strong>通过信道将生产者设置队列持久化</strong></p><pre><code>channel.queueDeclare(QUEUE_NAME,true,false,false,null);</code></pre></li><li><p><strong>通过信道将生产者设置队列的消息持久化</strong></p><pre><code>MessageProperties.PERSISTENT_TEXT_PLAIN(持久化)channel.basicPublish(exchange,queue, MessageProperties.PERSISTENT_TEXT_PLAIN,message);</code></pre></li></ul><h3 id="发布确认"><a href="#发布确认" class="headerlink" title="发布确认"></a>发布确认</h3><pre><code>    /**     * @author: yiLiang     * @Title: RabbitMQUtils     * 连接工厂创建信道     */    public class RabbitMQUtils {            // 队列的名称        public static final String QUEUE_NAME = "hello";        public static final String TASK_QUEUE_NAME = "ack_queue";            public static Channel getChannel(){            // 创建一个连接工厂            ConnectionFactory factory = new ConnectionFactory();            // 工厂IP 连接RabbitMQ的队列            factory.setHost("47.95.214.111");            // 设置用户名            factory.setUsername("guest");            // 密码            factory.setPassword("guest");                // 创建连接            try {                Connection connection = factory.newConnection();                // 获取信道                Channel channel = connection.createChannel();                return channel;            } catch (Exception e) {                e.printStackTrace();                return null;            }        }    }</code></pre><pre><code>    // 模拟发送1000条消息    public static Integer MESSAGE_COUNT = 1000;</code></pre><h4 id="单个确认"><a href="#单个确认" class="headerlink" title="单个确认"></a>单个确认</h4><pre><code>    /**       * 单个确认       * 1000条耗时大约 722ms(毫秒)    */    public static void publicMessageIndividually() throws Exception {        Channel channel = RabbitMQUtils.getChannel();        // 队列声明        String queueName = UUID.randomUUID().toString(); // 随机定义        channel.queueDeclare(queueName,true,false,false,null);        // 开启发布确认        channel.confirmSelect();        // 记录开始时间        long beginTime = System.currentTimeMillis();        for (Integer i = 0; i &lt; MESSAGE_COUNT; i++) {            String message = i + "";            // 发送消息            channel.basicPublish("",queueName,null,message.getBytes(StandardCharsets.UTF_8));            // 单个消息发布确认            boolean result = channel.waitForConfirms();            // result = true (发送成功)        }        // 记录结束时间        long endTime = System.currentTimeMillis();        System.out.println("发布耗时：" + (endTime-beginTime) + "ms(毫秒)");    }</code></pre><h4 id="批量确认"><a href="#批量确认" class="headerlink" title="批量确认"></a>批量确认</h4><pre><code>    /**       * 批量确认       * 1000条耗时大约 147ms(毫秒)    */    public static void publicMessageBatch() throws Exception{        Channel channel = RabbitMQUtils.getChannel();        // 队列声明        String queueName = UUID.randomUUID().toString(); // 随机定义        channel.queueDeclare(queueName,true,false,false,null);        // 开启发布确认        channel.confirmSelect();        // 记录开始时间        long beginTime = System.currentTimeMillis();        // 设置批量确认的长度        Integer batchSize = 100 ;        for (Integer i = 0; i &lt; MESSAGE_COUNT; i++) {            String message = i + "";            // 发送消息            channel.basicPublish("",queueName,null,message.getBytes(StandardCharsets.UTF_8));            // 判断达到 100 条消息时，批量确认一个            if (i%batchSize == 0){                // 发布确认                channel.waitForConfirms();            }        }        // 记录结束时间        long endTime = System.currentTimeMillis();        System.out.println("发布耗时：" + (endTime-beginTime) + "ms(毫秒)");    }</code></pre><h4 id="异步确认"><a href="#异步确认" class="headerlink" title="异步确认"></a>异步确认</h4><pre><code>    /**       * 异步确认       * 1000条耗时大约 62ms(毫秒)    */    public static void publicMessageAsync()throws Exception{        Channel channel = RabbitMQUtils.getChannel();        // 队列声明        String queueName = UUID.randomUUID().toString(); // 随机定义        channel.queueDeclare(queueName,true,false,false,null);        // 开启发布确认        channel.confirmSelect();        // 记录开始时间        long beginTime = System.currentTimeMillis();        /**           * ConfirmCallback           * 参数详解                * 1.消息的标识                * 2.是否批量确认        */        // 消息确认成功 回调函数        ConfirmCallback ackCallback = (deliveryTag, multiple) -&gt; {        };        // 消息确认失败 回调函数        ConfirmCallback nackCallback = (deliveryTag, multiple) -&gt; {            System.out.println("未确认的消息："+ deliveryTag);        };        /**           * 参数详解                * 1.消息确认成功                * 2.消息确认失败        */        // 准备消息的监听器 （监听消息发送失败与否） {异步通知}        channel.addConfirmListener(ackCallback, nackCallback);        // 批量发布        for (Integer i = 0; i &lt; MESSAGE_COUNT; i++) {            String message = i + "";            // 发送消息            channel.basicPublish("",queueName,null,message.getBytes(StandardCharsets.UTF_8));        }        // 记录结束时间        long endTime = System.currentTimeMillis();        System.out.println("发布耗时：" + (endTime-beginTime) + "ms(毫秒)");    }</code></pre><h3 id="处理异步未确认的消息"><a href="#处理异步未确认的消息" class="headerlink" title="处理异步未确认的消息"></a>处理异步未确认的消息</h3><pre><code>    /**       * 处理异步未确认消息    */    public static void ConcurrentLinkedQueue(){        // 将未确认消息放于一个基于内存的能被发布线下访问的队列        // ConcurrentLinkedQueue (并发链入式队列)        // 1.在发送消息时记录消息        // 2.在确认消息回调函数中删除确认的消息        // 在如上确认消息中的开启发布确认下添加        /**           * 线程安全有序的哈希表，适用于高并发的情况下           * &lt; 消息序号, 消息类型 &gt; ( &lt;key, value&gt; )                * 1.可以轻松的将序号与消息进行关联                * 2.轻松删除条码                * 3.支持高并发（多线程）        */        ConcurrentSkipListMap&lt;Long, String&gt; outstandingConfig = new ConcurrentSkipListMap&lt;&gt;();        // 记录消息        // outstandingConfig.put(channel.getNextPublishSeqNo(),message);        // 删除已经确认的消息            // 判断是否为批量确认消息        /*        if (multiple){            ConcurrentNavigableMap&lt;Long, String&gt; headMap = outstandingConfig.headMap(deliveryTag);            headMap.clear();        }else{            outstandingConfig.remove(deliveryTag);        }         */    }</code></pre>]]></content>
      
      
      <categories>
          
          <category> RabbitMQ </category>
          
      </categories>
      
      
        <tags>
            
            <tag> RabbitMQ </tag>
            
            <tag> 学习笔记 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>RabbitMQ的基本使用</title>
      <link href="/2020/03/20/RabbitMQ%E7%9A%84%E5%9F%BA%E6%9C%AC%E4%BD%BF%E7%94%A8/"/>
      <url>/2020/03/20/RabbitMQ%E7%9A%84%E5%9F%BA%E6%9C%AC%E4%BD%BF%E7%94%A8/</url>
      
        <content type="html"><![CDATA[<h2 id="创建队列"><a href="#创建队列" class="headerlink" title="创建队列"></a>创建队列</h2><pre><code>    // 队列名称    public static final String QUEUE_NAME = "hello";</code></pre><h2 id="生产者"><a href="#生产者" class="headerlink" title="生产者"></a>生产者</h2><pre><code>// 发消息    public static void main(String[] args) {        // 创建一个连接工厂        ConnectionFactory factory = new ConnectionFactory();        // 工厂IP 连接RabbitMQ的队列        factory.setHost("47.95.214.111");        // 设置用户名        factory.setUsername("guest");        // 密码        factory.setPassword("guest");        try {            // 创建连接            Connection connection = factory.newConnection();            // 获取信道            Channel channel = connection.createChannel();            /**               * 声明一个队列               * 参数详解                   * 1.队列名称                   * 2.队列消息是否持久化（存放磁盘中）（默认消息存储在内存中）                   * 3.该队列是否只供一个消费者消费 (false 只能一个消费者消费 true 可以多个消费者消费)                   * 4.是否自动删除 （最后一个消费者断开连接以后 false 否 true 自动删除）                   * 5. 其他参数（延迟、死信）               * 持久化： durable：true(持久) false(非持久)            */            channel.queueDeclare(QUEUE_NAME,true,false,false,null);            // 发布确认            channel.confirmSelect();            // 发送消息            String message = "hello world";            /**               * 发送消息               * 参数详解                    * 1.发送到的交换机                    * 2.路由的key（队列的名称）                    * 3.其他参数 (可以设置生产者消息持久化{保存在磁盘中}MessageProperties.PERSISTENT_TEXT_PLAIN)                    * 4.消息体           */            channel.basicPublish("",QUEUE_NAME, MessageProperties.PERSISTENT_TEXT_PLAIN,message.getBytes(StandardCharsets.UTF_8));            System.out.println("消息发送成功");        } catch (IOException e) {            e.printStackTrace();        } catch (TimeoutException e) {            e.printStackTrace();        }    }</code></pre><h2 id="消费者"><a href="#消费者" class="headerlink" title="消费者"></a>消费者</h2><pre><code>    /**       * 接收消息    */    public static void main(String[] args) {        // 创建一个连接工厂        ConnectionFactory factory = new ConnectionFactory();        // 工厂IP 连接RabbitMQ的队列        factory.setHost("47.95.214.111");        // 设置用户名        factory.setUsername("guest");        // 密码        factory.setPassword("guest");        try {            // 创建连接            Connection connection = factory.newConnection();            // 获取信道            Channel channel = connection.createChannel();            // 不公布分发(设置于消费者中)            // prefetchCount 可以设置预取值（因服务器性能设置）            channel.basicQos(1);            // 接收消息的回调            DeliverCallback deliverCallback = (consumerTag,message) -&gt;{                System.out.println(new String(message.getBody()));                /**                 * 手动应答                 * 参数详解                     * 1.消息的标识 tag                     * 2.是否批量应答  false:不批量 true:批量                 */                channel.basicAck(message.getEnvelope().getDeliveryTag(),false);            };            // 取消消息的回调            CancelCallback cancelCallback = consumerTag -&gt; {                System.out.println("消息消费被中断");            };            /**               * 消费者接收消息               * 参数详解                    * 1.消费队列                    * 2.消费成功之后是否自动应答 (true:自动应答 false:手动应答{建议使用手动应答})                    * 3.消费者未成功消费的回调                    * 4.消费者取消消费者的回调            */            channel.basicConsume(QUEUE_NAME,false,deliverCallback,cancelCallback);        } catch (IOException e) {            e.printStackTrace();        } catch (TimeoutException e) {            e.printStackTrace();        }    }</code></pre><h2 id="应答（手动应答、自动应答）"><a href="#应答（手动应答、自动应答）" class="headerlink" title="应答（手动应答、自动应答）"></a>应答（手动应答、自动应答）</h2><pre><code>        // 接收消息的回调        DeliverCallback deliverCallback = (consumerTag, message) -&gt;{            System.out.println(new String(message.getBody(),"UTF-8"));            /**               * 手动应答               * 参数详解                    * 1.消息的标识 tag                    * 2.是否批量应答  false:不批量 true:批量            */            channel.basicAck(message.getEnvelope().getDeliveryTag(),false);        };        // 取消消息的回调        CancelCallback cancelCallback = consumerTag -&gt; {            System.out.println(consumerTag + " \n ----消费者取消消息接口回调");        };        /**           * 消费者接收消息           * 参数详解                * 1.消费队列                * 2.消费成功之后是否自动应答 (true:自动应答 false:手动应答{建议使用手动应答})                * 3.消费者未成功消费的回调                * 4.消费者取消消费者的回调        */        channel.basicConsume(QUEUE_NAME,false,deliverCallback,cancelCallback);</code></pre>]]></content>
      
      
      <categories>
          
          <category> RabbitMQ </category>
          
      </categories>
      
      
        <tags>
            
            <tag> RabbitMQ </tag>
            
            <tag> 学习笔记 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>

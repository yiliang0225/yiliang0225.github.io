<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>HashMap</title>
      <link href="/2021/05/10/redis/"/>
      <url>/2021/05/10/redis/</url>
      
        <content type="html"><![CDATA[<h2 id="简概"><a href="#简概" class="headerlink" title="简概"></a>简概</h2><p>JDK1.8 之前HashMap是由数组+链表组成，数组是HashMap的主体，链表则是主要为了解决哈希冲突而存在的（“拉链法”解决冲突）。</p><p>JDK1.8以后HashMap是由数组+链表+红黑树组成，在解决哈希冲突有了较大的变化，当链表长度大于阈值（默认为8）（将链表转换成红黑树前会进行判断，如果当前数组的长度小于64，那么会选择先进行数组扩容，而不是转换为红黑树）时，将链表转化为红黑树，减少搜索时间。</p><h2 id="哈希表数据结构"><a href="#哈希表数据结构" class="headerlink" title="哈希表数据结构"></a>哈希表数据结构</h2><p>哈希表时一个数组和单项链表的结构体。数组在查询方面效率很高，随机增删方面效率低，而单向链表在随机增删方面效率较高，查询方面效率低。哈希表是将这两种数据结构融合在一起，充分发挥它们各自的优点。</p><h2 id="哈希冲突"><a href="#哈希冲突" class="headerlink" title="哈希冲突"></a>哈希冲突</h2><p>在进行数据存储时，通过对关键字进行hash时得到的地址已经存储过数据了，这时就会出现哈希冲突</p><p>哈希冲突的解决方案有多种:开放定址法（发生冲突，继续寻找下一块未被占用的存储地址），再散列函数法，拉链法，而HashMap即是采用了链地址法，也就是数组+链表的方式</p><h2 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h2><pre><code>为什么要改成“数组+链表+红黑树”？</code></pre><p>是为了提升在hash冲突时（链表过长）的查找性能，使用链表的查找性能是 O(n)，而使用红黑树是 O(logn)。</p><pre><code>什么时候用链表？什么时候用红黑树？</code></pre><p>对于插入，默认情况下是使用链表节点。当同一个索引位置的节点在新增后达到9个（阈值8）：如果此时数组长度大于等于 64，则会触发链表节点转红黑树节点（treeifyBin）；而如果数组长度小于64，则不会触发链表转红黑树，而是会进行扩容，因为此时的数据量还比较小。</p><p>对于移除，当同一个索引位置的节点在移除后达到 6 个，并且该索引位置的节点为红黑树节点，会触发红黑树节点转链表节点（untreeify）。</p><pre><code>为什么链表转红黑树的阈值是8？</code></pre><p>平时在进行方案设计时，必须考虑的两个很重要的因素是：时间和空间。对于 HashMap 也是同样的道理，简单来说，阈值为8是在时间和空间上权衡的结果.</p><pre><code>HashMap 有哪些重要属性？分别用于做什么的？</code></pre><p>除了用来存储我们的节点 table 数组外，HashMap 还有以下几个重要属性：1）size：HashMap 已经存储的节点个数；2）threshold：扩容阈值，当 HashMap 的个数达到该值，触发扩容。3）loadFactor：负载因子，扩容阈值 = 容量 * 负载因子。</p><pre><code>HashMap 的默认初始容量是多少？HashMap 的容量有什么限制吗？</code></pre><p>默认初始容量是16。HashMap 的容量必须是2的N次方，HashMap 会根据我们传入的容量计算一个大于等于该容量的最小的2的N次方，例如传 9，容量为16。</p>]]></content>
      
      
      <categories>
          
          <category> Map </category>
          
      </categories>
      
      
        <tags>
            
            <tag> HashMap </tag>
            
            <tag> 学习笔记 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hexo 博客搭建</title>
      <link href="/2021/03/10/Hexo%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA/"/>
      <url>/2021/03/10/Hexo%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA/</url>
      
        <content type="html"><![CDATA[<h2 id="安装Nodejs"><a href="#安装Nodejs" class="headerlink" title="安装Nodejs"></a>安装Nodejs</h2><pre><code>node -v    #查看node版本npm -v    #查看npm版本</code></pre><h3 id="安装淘宝-CNPM"><a href="#安装淘宝-CNPM" class="headerlink" title="安装淘宝 CNPM"></a>安装淘宝 CNPM</h3><pre><code>npm install -g cnpm --registry=http://registry.npm.taobao.org    #安装淘宝的cnpm 管理器cnpm -v    #查看cnpm版本</code></pre><h3 id="安装-HEXO-框架"><a href="#安装-HEXO-框架" class="headerlink" title="安装 HEXO 框架"></a>安装 HEXO 框架</h3><pre><code>cnpm install -g hexo-cli    #安装hexo框架hexo -v    #查看hexo版本</code></pre><h3 id="创建-blog"><a href="#创建-blog" class="headerlink" title="创建 blog"></a>创建 blog</h3><pre><code>mkdir blog    #创建blog目录cd blog     #进入blog目录sudo hexo init     #生成博客 初始化博客hexo s    #启动本地博客服务http://localhost:4000/    #本地访问地址</code></pre><h3 id="创建新文章"><a href="#创建新文章" class="headerlink" title="创建新文章"></a>创建新文章</h3><pre><code>hexo n "我的第一篇文章"  #创建新的文章  ""里面的为blog名称</code></pre><h3 id="返回blog目录"><a href="#返回blog目录" class="headerlink" title="返回blog目录"></a>返回blog目录</h3><pre><code>hexo clean #清理hexo g #生成</code></pre><h3 id="创建GitHub仓库"><a href="#创建GitHub仓库" class="headerlink" title="创建GitHub仓库"></a>创建GitHub仓库</h3><p><img src="/img/hexo_gitHob.png"></p><pre><code>YourGithubName.github.io</code></pre><h3 id="安装git部署插件"><a href="#安装git部署插件" class="headerlink" title="安装git部署插件"></a>安装git部署插件</h3><pre><code>cnpm install --save hexo-deployer-git #在blog目录下安装git部署插件</code></pre><hr><h3 id="配置-config-yml"><a href="#配置-config-yml" class="headerlink" title="配置_config.yml"></a>配置_config.yml</h3><pre><code># Deployment## Docs: https://hexo.io/docs/deployment.htmldeploy:      type: git     repo: https://github.com/YourGithubName/YourGithubName.github.io.git      branch: master</code></pre><pre><code>hexo d    #部署到Github仓库里https://YourGithubName.github.io/  #访问这个地址可以查看博客</code></pre><h3 id="主题"><a href="#主题" class="headerlink" title="主题"></a>主题</h3><pre><code>git clone https://github.com/litten/hexo-theme-yilia.git themes/yilia  #下载yilia主题到本地</code></pre><p>修改hexo根目录下的 _config.yml 文件 ： theme: yilia</p><h3 id="Hexo-三连"><a href="#Hexo-三连" class="headerlink" title="Hexo 三连"></a>Hexo 三连</h3><pre><code>hexo clean #清理hexo g #生成hexo s #运行</code></pre><pre><code>hexo c    #清理一下hexo g    #生成hexo d    #部署到远程Github仓库</code></pre>]]></content>
      
      
      <categories>
          
          <category> hexo </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 博客搭建 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>秒杀系统</title>
      <link href="/2020/10/10/%E7%A7%92%E6%9D%80/"/>
      <url>/2020/10/10/%E7%A7%92%E6%9D%80/</url>
      
        <content type="html"><![CDATA[<h2 id="一、设计"><a href="#一、设计" class="headerlink" title="一、设计"></a>一、设计</h2><p>库存预热：<br>提前把商品添加到redis中，整个秒杀流程都在redis中操作，秒杀结束，在异步去修改库存。  </p><p>用户下单：<br>为了提升下单速度，将订单存放的redis中，用户支付后，再将订单存到mysql中，并清空redis中的订单。  </p><p>创建订单：<br>用户每次下单的时候，使用rabbitMQ创建一个队列，采用异步的方式创建订单，从rabbitMQ中读取信息，创建订单。  </p><p>下单状态：<br>做一个页面的判断，每个1s查询下单状态。    </p><p>超卖问题：<br>数据库添加唯一索引，减库存的时候先进行库存数量判断；用redis来维护库存，redis中的库存不足则秒杀失败；异步队列处理订单。  </p><span id="more"></span><h2 id="二、缓存"><a href="#二、缓存" class="headerlink" title="二、缓存"></a>二、缓存</h2><p>在高并发的情况下必然会遇到<strong>缓存雪崩</strong>、<strong>缓存击穿</strong>、<strong>缓存穿透</strong>等问题。而且秒杀是<strong>读多写少</strong>，使用redis作为缓存非常合适（为了避免redis服务器出问题，可以使用redis集群）。  </p><h3 id="2-1、缓存穿透"><a href="#2-1、缓存穿透" class="headerlink" title="2.1、缓存穿透"></a>2.1、缓存穿透</h3><pre><code>描述：是指缓存和数据库中没有数据，而用户不断请求，会导致数据库压力过大解决方案：  1、在接口层校验，如果用户鉴权校验，id为基础校验，id&lt;=0的直接拦截2、从缓存取不到数据，在数据库中也没有取到，可以将key-value对写成key-null，将缓存有效时间可以设置短点，设置太长会导致无法正常使用。</code></pre><h3 id="2-2、缓存击穿"><a href="#2-2、缓存击穿" class="headerlink" title="2.2、缓存击穿"></a>2.2、缓存击穿</h3><pre><code>描述：是指缓存中没有数据但是数据库中有数据（一般是缓存时间到期），这是由于并发用户特别多，同时读缓存没有读到数据，有同时去请求数据库，引起数据库压力瞬间增大。解决方案：1、设置热点永不过期2、加互斥锁</code></pre><pre><code>    public String getData(String key) throws InterruptedException {        // 创建锁        ReentrantLock reenLock = new ReentrantLock();        // 从缓存中读取数据        String result = getDataFromRedis(key);        // 缓存中不存在数据        if (result == null){            // 获取锁，获取成功去数据库中取数据            if (reenLock.tryLock()){                // 从数据库获取数据                result = getDataFromMysql(key);                // 更新缓存                if (result != null){                    setDataToCache(key,result);                }                // 释放锁                reenLock.unlock();            }else{                // 暂停一会，在重新去获取                Thread.sleep(100);                result = getData(key);            }        }        return result;    }</code></pre><h3 id="2-3、缓存雪崩"><a href="#2-3、缓存雪崩" class="headerlink" title="2.3、缓存雪崩"></a>2.3、缓存雪崩</h3><pre><code>描述：是指缓存中数据大量到过期时间，而查询数据量巨大，引起数据库压力过大甚至宕机  解决方案：  1、缓存数据的过期时间设置随机，防止同一时间大量数据过期。  2、缓存数据库是分布式部署，将热点数据均匀分布不同缓存数据库中  3、设置热点数据永不过期</code></pre><h2 id="三、削峰-x2F-限流"><a href="#三、削峰-x2F-限流" class="headerlink" title="三、削峰/限流"></a>三、削峰/限流</h2><p>秒杀流量在某一时刻非常高，让瞬间进来的流量进到一个缓存池，然后进行平缓处理（mq消息队列处理）。</p><pre><code>@Configurationpublic class RabbitConfig {    private static final Logger logger = LoggerFactory.getLogger(RabbitConfig.class);    // 交换机    public final static String EXCHANGE_NAME = "order:mq:exchange";    // 队列    public final static String QUEUE_NAME = "order:mq:queue";    // 路由键    public final static String ROUTING_KEY = "order:mq:routing:key";    /**     * channel链接工厂     */    @Autowired    private CachingConnectionFactory connectionFactory;    /**     * 监听器容器配置     */    @Autowired    private SimpleRabbitListenerContainerFactoryConfigurer factoryConfigurer;    /**     * 声明rabbittemplate     * @return     */    @Bean    public RabbitTemplate rabbitTemplate(){        //消息发送成功确认，对应application.properties中的spring.rabbitmq.publisher-confirms=true        connectionFactory.setPublisherConfirms(true);        //消息发送失败确认，对应application.properties中的spring.rabbitmq.publisher-returns=true        connectionFactory.setPublisherReturns(true);        RabbitTemplate rabbitTemplate = new RabbitTemplate(connectionFactory);        //设置消息发送格式为json        rabbitTemplate.setMessageConverter(new Jackson2JsonMessageConverter());        rabbitTemplate.setMandatory(true);        //消息发送到exchange回调 需设置：spring.rabbitmq.publisher-confirms=true        rabbitTemplate.setConfirmCallback(new RabbitTemplate.ConfirmCallback() {            @Override            public void confirm(CorrelationData correlationData, boolean ack, String cause) {                logger.info("消息发送成功:correlationData({}),ack({}),cause({})",correlationData,ack,cause);            }        });        //消息从exchange发送到queue失败回调  需设置：spring.rabbitmq.publisher-returns=true        rabbitTemplate.setReturnCallback(new RabbitTemplate.ReturnCallback() {            @Override            public void returnedMessage(Message message, int replyCode, String replyText, String exchange, String routingKey) {                logger.info("消息丢失:exchange({}),route({}),replyCode({}),replyText({}),message:{}",exchange,routingKey,replyCode,replyText,message);            }        });        return rabbitTemplate;    }    //---------------------------------------订单队列------------------------------------------------------    /**     * 声明订单队列的交换机     * @return     */    @Bean    public TopicExchange orderTopicExchange(){        //设置为持久化 不自动删除        return new TopicExchange(EXCHANGE_NAME,true,false);    }    /**     * 声明订单队列     * @return     */    @Bean    public Queue orderQueue(){        return new Queue(QUEUE_NAME,true);    }    /**     * 将队列绑定到交换机     * @return     */    @Bean    public Binding simpleBinding(){        return BindingBuilder.bind(orderQueue()).to(orderTopicExchange()).with(ROUTING_KEY);    }}</code></pre><pre><code>    // 生产消息    public String send(String message){        rabbitTemplate.convertAndSend(RabbitConfig.EXCHANGE_NAME,RabbitConfig.ROUTING_KEY,message);        retuen "消息发送成功";    }            // 配置rabbitmq监听    /**     * 处理接收到的消息     * @param message 消息体     * @param channel 通道，确认消费用     */    @RabbitListener(queues = RabbitConfig.QUEUE_NAME)    public void onMessage(Message message, com.rabbitmq.client.Channel channel){        try{            //获取交付tag            long tag = message.getMessageProperties().getDeliveryTag();            String str = new String(message.getBody(),"utf-8");            logger.info("接收到的消息：{}",str);            //确认消费            channel.basicAck(tag,true);        }catch(Exception e){            logger.error("消息监听确认机制发生异常：",e.fillInStackTrace());        }    }</code></pre><h3 id="并发配置"><a href="#并发配置" class="headerlink" title="并发配置"></a>并发配置</h3><pre><code>    /**       * 容器工厂配置 并发       * @return    */    @Bean("containerFactory")    public RabbitListenerContainerFactory rabbitListenerContainerFactory(ConnectionFactory connectionFactory){        SimpleRabbitListenerContainerFactory listenerContainerFactory = new SimpleRabbitListenerContainerFactory();        listenerContainerFactory.setConnectionFactory(connectionFactory);        listenerContainerFactory.setMessageConverter(new Jackson2JsonMessageConverter());        // 最大并发数        listenerContainerFactory.setMaxConcurrentConsumers(20);        // 初始并发数        listenerContainerFactory.setConcurrentConsumers(10);        return listenerContainerFactory;    }            // containerFactory 指定容器工厂        @RabbitListener(queues = RabbitConfig.QUEUE_NAME,containerFactory = "containerFactory")   </code></pre>]]></content>
      
      
      <categories>
          
          <category> 秒杀 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 学习笔记 </tag>
            
            <tag> 秒杀 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>RabbitMQ 如何保证消息的可靠传输</title>
      <link href="/2020/07/11/RabbitMQ%20%E5%A6%82%E4%BD%95%E4%BF%9D%E8%AF%81%E6%B6%88%E6%81%AF%E7%9A%84%E5%8F%AF%E9%9D%A0%E4%BC%A0%E8%BE%93/"/>
      <url>/2020/07/11/RabbitMQ%20%E5%A6%82%E4%BD%95%E4%BF%9D%E8%AF%81%E6%B6%88%E6%81%AF%E7%9A%84%E5%8F%AF%E9%9D%A0%E4%BC%A0%E8%BE%93/</url>
      
        <content type="html"><![CDATA[<h4 id="可靠传输"><a href="#可靠传输" class="headerlink" title="可靠传输"></a>可靠传输</h4><p>在RabbitMQ中，一个消息从产生到被消费大致会经过三个步骤，生产者生产消息，消息投递到RabbitMQ，RabbitMQ再将消息推送到消费者（或消费者拉取），最终消费者将这条消息成功消费。消息的可靠传输，就是确保消息能够百分百从生产者发送到服务器，在从服务器发送到消费者。</p><p>所以消息丢失也可以划分为三种情况——生产者、消息队列、消费者。</p><span id="more"></span><h4 id="生产者投递消息失败"><a href="#生产者投递消息失败" class="headerlink" title="生产者投递消息失败"></a>生产者投递消息失败</h4><h5 id="事务机制"><a href="#事务机制" class="headerlink" title="事务机制"></a>事务机制</h5><p>使用 RabbitMQ 的事务功能，此时可以选择用 RabbitMQ 提供的事务功能，就是生产者发送数据之前开启 RabbitMQ 事务 channel.txSelect，然后发送消息，如果消息没有成功被 RabbitMQ 接收到，那么生产者会收到异常报错，此时就可以回滚事务 channel.txRollback，然后重试发送消息；如果收到了消息，那么可以提交事务 channel.txCommit。</p><pre class=" language-java"><code class="language-java">`<span class="token comment" spellcheck="true">// 开启事务`</span>`channel<span class="token punctuation">.</span><span class="token function">txSelect</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>``<span class="token keyword">try</span> <span class="token punctuation">{</span>`    `<span class="token comment" spellcheck="true">// 发送消息`</span>`<span class="token punctuation">}</span> <span class="token keyword">catch</span><span class="token punctuation">(</span>Exception e<span class="token punctuation">)</span> <span class="token punctuation">{</span>`    `channel<span class="token punctuation">.</span><span class="token function">txRollback</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>`    `<span class="token comment" spellcheck="true">// 重发消息`</span>`<span class="token punctuation">}</span>``<span class="token comment" spellcheck="true">// 提交事务`</span>`channel<span class="token punctuation">.</span><span class="token function">txCommit</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>`</code></pre><p>事务机制可以确保生产者投递消息成功，但是这种方式有比较大的缺点，太耗性能。</p><h5 id="confirm机制"><a href="#confirm机制" class="headerlink" title="confirm机制"></a>confirm机制</h5><p>要确保 RabbitMQ 生产者的消息正确投递，可以开启 confirm 模式，在生产者端设置开启 confirm 模式之后，你每次写的消息都会分配一个唯一的 id，然后如果写入了 RabbitMQ 中，RabbitMQ 会给你回传一个 ACK 消息，告诉你说这个消息。</p><p>如果 RabbitMQ 没能处理这个消息，会回调你一个 nack 接口，告诉你这个消息接收失败，你可以重试。而且你可以结合这个机制自己在内存里维护每个消息 id 的状态，如果超过一定时间还没接收到这个消息的回调，那么你可以重发。</p><pre class=" language-java"><code class="language-java"><span class="token keyword">try</span> <span class="token punctuation">{</span>    channel<span class="token punctuation">.</span><span class="token function">confirmSelect</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment" spellcheck="true">//将信道置为 publisher confirm 模式</span>    <span class="token comment" spellcheck="true">// 之后正常发送消息</span>    channel<span class="token punctuation">.</span><span class="token function">basicPushlish</span><span class="token punctuation">(</span><span class="token string">"exchange"</span><span class="token punctuation">,</span> <span class="token string">"routingKey"</span><span class="token punctuation">,</span> null<span class="token punctuation">,</span>                           <span class="token string">"publisher confirm test"</span><span class="token punctuation">.</span><span class="token function">getBytes</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>    <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span>channel<span class="token punctuation">.</span><span class="token function">waitFormConfirms</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>        System<span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token string">"Send message failed"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>        <span class="token comment" spellcheck="true">// do something else...</span>    <span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">InterruptedException</span> e<span class="token punctuation">)</span> <span class="token punctuation">{</span>        e<span class="token punctuation">.</span><span class="token function">printStackTrace</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>    <span class="token punctuation">}</span><span class="token punctuation">}</span></code></pre><p>事务机制和 confirm 机制最大的不同在于，事务机制是同步的，你提交一个事务之后会阻塞在那儿，但是 confirm 机制是异步的，你发送个消息之后就可以发送下一个消息，然后那个消息 RabbitMQ 接收之后会异步回调你一个接口通知你这个消息接收到了。</p><p>所以一般生产者到 RabbitMQ 这块避免数据丢失，会采用 confirm 机制更多些。</p><h5 id="消息队列自身丢失"><a href="#消息队列自身丢失" class="headerlink" title="消息队列自身丢失"></a>消息队列自身丢失</h5><p>就是 RabbitMQ 自己弄丢了数据，这个你必须开启 RabbitMQ 的持久化，就是消息写入之后会持久化到磁盘，哪怕是 RabbitMQ 自己挂了，恢复之后会自动读取之前存储的数据，一般数据不会丢。除非极其罕见的是，RabbitMQ 还没持久化，自己就挂了，可能导致少量数据丢失，但是这个概率较小。</p><p>设置持久化有两个步骤：</p><ul><li><strong>创建 queue 的时候将其设置为持久化</strong></li></ul><p>这样就可以保证 RabbitMQ 持久化 queue 的元数据，但是不会持久化 queue 里的数据。</p><ul><li><strong>发送消息时将消息的 deliveryMode 设置为 2</strong></li></ul><p>就是将消息设置为持久化，此时 RabbitMQ 就会将消息持久化到磁盘上去。</p><p>必须要同时设置这两个持久化才行，RabbitMQ 哪怕是挂了，再次重启，也会从磁盘上重启恢复 queue，恢复这个 queue 里的数据。</p><p>同时持久化也可以跟生产者那边的 confirm 机制配合起来，只有消息被持久化到磁盘后，才会通知生产者 ack 了，所以哪怕是在持久化到磁盘之前，RabbitMQ 挂了，数据丢了，生产者收不到 ACK，你也是可以自己重发的。</p><p>注意，哪怕是你给 RabbitMQ 开启了持久化机制，也有一种可能，就是这个消息写到了 RabbitMQ 中，但是还没来得及持久化到磁盘上，结果不巧，此时 RabbitMQ 挂了，就会导致内存中的数据丢失。</p><h5 id="消费者宕机"><a href="#消费者宕机" class="headerlink" title="消费者宕机"></a>消费者宕机</h5><p>对于消费者端所产生的情况就是：消费者成功接收到消息，但是还未将消息处理完毕就宕机了。针对这种情况，可以利用 RabbitMQ 提供的<strong>消息确认机制</strong>。</p><p>为了保证消息从队列可靠地到达消费者，RabbitMQ 提供了消息确认机制（message acknowledgement）。消费者在订阅队列时，可以指定 autoAck 参数：</p><ul><li><p>当 autoAck 等于 true 时，RabbitMQ 会自动把发送出去的消息置为确认，然后从内存（或者磁盘）中删除，而不管消费者是否真正地消费到了这些消息；</p></li><li><p>当 autoAck 等于 false 时，RabbitMQ 会等待消费者显示地回复确认信号后才从内存（后者磁盘）中移去消息（实质上是先打上删除标记，之后再删除）。</p></li></ul><p>所以对于消费者可能发生宕机地情况，我们可以将 autoAck 参数置为 false，消费者就有足够的时间处理这条消息，不用担心处理消息过程中消费者进程挂掉后消息丢失的问题，因为 RabbitMQ 会一直等待并且持有这条消息，直到消费者显示调用 Basic.Ack 命令为止。</p><p>当 autoAck 参数设置为 false，对于 RabbitMQ 服务端而言，队列中的消息分成了两个部分：一部分是等待投递给消费者的消息，另一部分是已经投递给消费者，但是还没有收到消费者确认信号的消息。如果 RabbitMQ 一直没有收到消费者的确认信号，并且消费此消息的消费者已经断开连接，则 RabbitMQ 会安排该消息重新进入队列，等待投递给下一个消费者，当然也可能还是原来的那个消费者。</p><p><a href="https://blog.csdn.net/Allen_Adolph/article/details/109055174">原文链接</a></p>]]></content>
      
      
      <categories>
          
          <category> RabbitMQ </category>
          
          <category> 可靠传输 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 学习笔记 </tag>
            
            <tag> RabbitMQ </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
